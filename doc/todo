. file loading
 . use mpn_set_str / get_str
  . preallocate in the dest file an call get_str(dest_buf)

. // reading of A + processing
. gpu operations
. better range handling
. use mpn low level functions

. kaapi optims
 . use ktr to allocate data and kaapi_preemptpoint
 no longer recopies data, as for kastl_loop
 . or make ktr points on the stack directly, but doesnot remove last copy

. if matrix is too big, divide it and distribute?
