. bignum library
. find big matrices to test
. adjust grains
. split per block

. use muladd in matrix multiplication

. matrix.c
 . line should not be reallocated each time
 . check write return vals

. optims
 . use ktr to allocate data and kaapi_preeemptpoint
 no longer recopies data, as for kastl_loop
 . or make ktr points on the stack directly, but doesnot remove last copy
